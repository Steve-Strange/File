#include<iostream>
using namespace std;

int main(){
	int a=5,b=5;
	cout<<(a==b)<<endl<<(a>b)<<endl<<(a<=b)<<endl; 
	// 判断式子真假  如为真，结果为1  假为0  == 等于  != 不等于  < >  <=  >=  
	//cout<<()<<  括号内优先  优先判断真假，返回0 1  而非优先输出 a 
	
	system("pause");
	return 0;
}
